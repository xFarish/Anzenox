import { Anzen } from '../../types/global';

// @ts-ignore
export function parser(ast: Array<Anzen.Token>) {
    // @ts-ignore
    let count = 0;

    // @ts-ignore
    function walk(): Anzen.Body | Anzen.Node | never {
        
    }
}